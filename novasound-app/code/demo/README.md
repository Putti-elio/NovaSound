# NovaSound demo

This directory reserves the future static GitHub Pages demonstration. It must
reuse the shared UI from `../ui/` and replace remote API calls with the
non-sensitive fixtures in `../ui/src/mocks/`.

The demo must not require authentication, a backend or database connection,
provider credentials, user data, or any other secret. Its build and Pages
deployment workflow are deliberately not implemented yet.
