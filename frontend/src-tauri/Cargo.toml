[package]
name = "novasound"
version = "0.1.0"
description = "NovaSound desktop application"
authors = ["NovaSound"]
edition = "2024"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
