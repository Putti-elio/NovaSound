{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability without native permissions.",
  "windows": ["main"],
  "permissions": []
}
